linters: linters_with_defaults(
    line_length_linter(180),
    indentation_linter(indent = 2),
    object_name_linter = NULL,
    brace_linter = NULL,
    object_usage_linter = NULL,
    seq_linter = NULL,
    commas_linter = NULL,
    pipe_continuation_linter = NULL
  )
